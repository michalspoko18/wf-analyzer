WF_LOGIN=
WF_PASSWORD=

# PostgreSQL connection string — external server
DATABASE_URL=postgresql://user:password@host:5432/wf_analyzer

# Public URL of the API — without /api suffix (api.ts appends it)
# For local Docker: http://localhost:8000
FRONTEND_API_URL=http://localhost:8000
