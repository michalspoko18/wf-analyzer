WF_LOGIN=
WF_PASSWORD=

# PostgreSQL connection string — external server
DATABASE_URL=postgresql://user:password@host:5432/wf_analyzer

# Secret key protecting the API — use a long random string in production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
API_KEY=change-me-to-a-random-secret
